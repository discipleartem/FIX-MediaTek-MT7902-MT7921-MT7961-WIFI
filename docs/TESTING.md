# 🧪 Тестирование MT7921 WiFi

## 📋 Обзор

Этот документ описывает процедуры тестирования WiFi адаптера MediaTek MT7921 после установки патча.

## 🔍 Базовая проверка

### 1. Проверка определения устройства

```bash
# Проверка PCI устройства
lspci -nn | grep -i mediatek

# Ожидаемый вывод:
# 03:00.0 Network controller [0280]: MEDIATEK Device [14c3:7902]
```

### 2. Проверка загрузки модуля

```bash
# Проверка загруженных модулей
lsmod | grep mt7921

# Ожидаемый вывод:
# mt7921_pci             40960  0
# mt7921_common          69632  1 mt7921_pci
# mt76_connac_lib        49152  1 mt7921_common
# mt76                   65536  2 mt7921_pci,mt7921_common
# cfg80211              942080  5 mt7921_pci,mt7921_common,mt76_connac_lib,mt76
```

### 3. Проверка сетевого интерфейса

```bash
# Проверка сетевых интерфейсов
ip link show

# Ожидаемый вывод:
# 3: wlan0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc mq state UP mode DORMANT group default qlen 1000
#     link/ether xx:xx:xx:xx:xx:xx brd ff:ff:ff:ff:ff:ff
```

## 📶 Тестирование WiFi

### 1. Сканирование сетей

```bash
# Сканирование доступных сетей
nmcli dev wifi list

# Или с помощью iwlist
iwlist wlan0 scan
```

### 2. Подключение к сети

```bash
# Подключение через NetworkManager
nmcli dev wifi connect "SSID_NAME" password "PASSWORD"

# Или через wpa_supplicant
wpa_supplicant -B -i wlan0 -c <(wpa_passphrase "SSID_NAME" "PASSWORD")
dhclient wlan0
```

### 3. Проверка соединения

```bash
# Проверка IP адреса
ip addr show wlan0

# Проверка шлюза
ip route show

# Проверка интернета
ping -c 4 8.8.8.8
ping -c 4 google.com
```

## 🔬 Детальное тестирование

### 1. Тест скорости

```bash
# Установка iperf3
sudo apt install iperf3

# Тест скорости (требует iperf3 сервер)
iperf3 -c iperf.server.com -t 30

# Или тест с публичным сервером
iperf3 -c speedtest.net -t 10
```

### 2. Тест стабильности

```bash
# Длительный пинг
ping -i 1 -c 3600 8.8.8.8 > ping_test.log &

# Проверка через час
tail ping_test.log
```

### 3. Тест разных режимов

```bash
# Проверка поддерживаемых режимов
iw phy phy0 info | grep "Supported modes"

# Проверка частот
iwlist wlan0 channel

# Проверка мощности передачи
iwconfig wlan0
```

## 📊 Анализ производительности

### 1. Мониторинг сигнала

```bash
# Мониторинг уровня сигнала
watch -n 1 "cat /proc/net/wireless"

# Или с помощью iw
iw dev wlan0 link
```

### 2. Статистика драйвера

```bash
# Статистика mt7921
cat /sys/kernel/debug/ieee80211/phy0/mt76/debug

# Счетчики производительности
cat /proc/net/dev | grep wlan0
```

## 🐛 Диагностика проблем

### 1. Проверка логов

```bash
# Логи загрузки модуля
dmesg | grep mt7921

# Логи NetworkManager
journalctl -u NetworkManager | grep wlan0

# Логи wpa_supplicant
journalctl -u wpa_supplicant | grep wlan0
```

### 2. Проверка firmware

```bash
# Проверка загруженной firmware
dmesg | grep "mt7921 firmware"

# Проверка файлов firmware
ls -la /lib/firmware/mediatek/mt7921*
```

### 3. Проверка регуляторных доменов

```bash
# Текущий регуляторный домен
iw reg get

# Проверка ограничений
iw phy phy0 info | grep -A 10 "Frequencies"
```

## 📋 Чек-лист тестирования

### ✅ Базовая функциональность
- [ ] Устройство определяется в PCI
- [ ] Модуль mt7921 загружается
- [ ] Интерфейс wlan0 создается
- [ ] Сканирование сетей работает
- [ ] Подключение к сети работает
- [ ] Интернет доступен

### ✅ Производительность
- [ ] Скорость загрузки > 50 Mbps
- [ ] Скорость отдачи > 10 Mbps
- [ ] Пинг < 50ms до локальных серверов
- [ ] Стабильное соединение > 1 час

### ✅ Дополнительные функции
- [ ] 5GHz работает (если поддерживается)
- [ ] WPA2/WPA3 работает
- [ ] Смена каналов работает
- [ ] Режим точки доступа работает

## 📞 Отчет о проблемах

При создании отчета о проблеме включите:

```bash
#!/bin/bash
echo "=== MT7921 Debug Info ==="
echo "Дата: $(date)"
echo "Система: $(uname -a)"
echo ""
echo "=== PCI Устройство ==="
lspci -nn | grep -i mediatek
echo ""
echo "=== Загруженные модули ==="
lsmod | grep mt7921
echo ""
echo "=== Сетевые интерфейсы ==="
ip link show | grep -A 1 wlan
echo ""
echo "=== Логи ядра ==="
dmesg | grep -i mt7921 | tail -20
echo ""
echo "=== Версия firmware ==="
ls -la /lib/firmware/mediatek/mt7921* 2>/dev/null || echo "Firmware не найдена"
echo ""
echo "=== Текущее соединение ==="
iw dev wlan0 link 2>/dev/null || echo "Нет активного соединения"
```

---

**Версия документа:** 1.0.0  
**Последнее обновление:** 2026-02-12
